import React, { ReactNode } from 'react';
import '../tailwind.css'
import { motion, AnimatePresence } from 'framer-motion'

type SearchProps<T> = {

}

export function Search<T extends ReactNode & {}>(props: SearchProps<T>) {
    return <div></div>
}